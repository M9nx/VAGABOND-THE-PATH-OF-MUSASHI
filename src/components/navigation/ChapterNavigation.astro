---
import { chapters } from '~/data/chapters';

const navItems = [
  { id: 'introduction', number: '00', label: 'Introduction' },
  ...chapters.map((chapter) => ({
    id: chapter.id,
    number: chapter.number,
    label: chapter.navigationLabel,
  })),
];
---

<ul class="site-nav__links" aria-label="Chapters">
  {
    navItems.map((item) => (
      <li>
        <a
          class="site-nav__link"
          href={`#${item.id}`}
          data-nav-item={item.id}
          aria-current="false"
        >
          <span class="site-nav__link-number">{item.number}</span>
          <span class="site-nav__link-label">{item.label}</span>
        </a>
      </li>
    ))
  }
</ul>
